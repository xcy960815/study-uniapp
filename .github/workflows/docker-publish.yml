name: Docker Build and Push

on:
  push:
    tags:
      - 'v*' # 当推送 v 开头的标签时触发，例如 v1.0.0

env:
  # 替换为你的 Docker Hub 用户名/组织名
  IMAGE_NAME: xcy960815/study-uniapp

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64 # 如果需要多架构构建
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ⚠️ 暂时关闭：从 README 自动更新 Docker Hub Overview
      - name: Update Docker Hub repository overview from README
        env:
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          # 将 README.md 转成 JSON 安全的字符串
          DESCRIPTION=$(jq -Rs . < README.md)

          # 调用 Docker Hub API 更新仓库 full_description（Overview）
          curl --fail --silent --show-error \
            --request PATCH \
            --url https://hub.docker.com/v2/repositories/xcy960815/study-uniapp/ \
            --header "Content-Type: application/json" \
            --user "${{ secrets.DOCKERHUB_USERNAME }}:${DOCKERHUB_TOKEN}" \
            --data "{\"full_description\": ${DESCRIPTION}}"
